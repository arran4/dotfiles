name: Hyprland Lua validation

on:
  pull_request:
    paths:
      - 'dot_config/hypr/**'
      - '.github/workflows/hyprland-lua-dispatcher-api.yml'
  push:
    branches:
      - main
    paths:
      - 'dot_config/hypr/**'
      - '.github/workflows/hyprland-lua-dispatcher-api.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Lua
        run: sudo apt-get update && sudo apt-get install -y lua5.4
      - name: Validate Hyprland Lua dispatcher API usage
        shell: python
        run: |
          import pathlib
          import re
          import sys

          paths = [
              pathlib.Path('dot_config/hypr/hyprland.lua.tmpl'),
              pathlib.Path('dot_config/hypr/special_workspaces.lua'),
          ]
          sources = {path: path.read_text() for path in paths if path.exists()}

          allowed_top_level = {
              'exec_cmd',
              'focus',
              'global',
              'layout',
              'window',
              'workspace',
          }
          allowed_window = {
              'alter_zorder',
              'close',
              'cycle_next',
              'drag',
              'float',
              'fullscreen',
              'move',
              'resize',
          }
          allowed_workspace = {
              'toggle_special',
          }

          errors = []
          for path, source in sources.items():
              for match in re.finditer(r'hl\.dsp\.layout\.([A-Za-z_]\w*)', source):
                  errors.append(
                      f'{path}:{source.count(chr(10), 0, match.start()) + 1}: '
                      f'invalid nested layout dispatcher hl.dsp.layout.{match.group(1)}; '
                      'use hl.dsp.layout("message")'
                  )

              for namespace, allowed in (
                  ('window', allowed_window),
                  ('workspace', allowed_workspace),
              ):
                  pattern = rf'hl\.dsp\.{namespace}\.([A-Za-z_]\w*)'
                  for match in re.finditer(pattern, source):
                      method = match.group(1)
                      if method not in allowed:
                          errors.append(
                              f'{path}:{source.count(chr(10), 0, match.start()) + 1}: '
                              f'unknown/unreviewed dispatcher hl.dsp.{namespace}.{method}'
                          )

              for match in re.finditer(r'hl\.dsp\.([A-Za-z_]\w*)', source):
                  name = match.group(1)
                  if name not in allowed_top_level:
                      errors.append(
                          f'{path}:{source.count(chr(10), 0, match.start()) + 1}: '
                          f'unknown/unreviewed top-level dispatcher hl.dsp.{name}'
                      )

              forbidden = {
                  'hl.dsp.window.toggle_floating': 'use hl.dsp.window.float({ action = "toggle" })',
                  'hl.dsp.layout.toggle_split': 'use hl.dsp.layout("togglesplit")',
              }
              for token, replacement in forbidden.items():
                  if token in source:
                      errors.append(f'{path}: forbidden {token}; {replacement}')

          if errors:
              print('\n'.join(errors), file=sys.stderr)
              sys.exit(1)

          print('Hyprland Lua dispatcher API usage is valid')
      - name: Verify special workspace routing is global
        run: |
          grep -Fq 'require("special_workspaces")' dot_config/hypr/hyprland.lua.tmpl
          ! grep -Fq 'require("special_workspaces")' dot_config/hypr/local_x1linux.lua
          ! grep -Fq 'require("special_workspaces")' dot_config/hypr/local_desktop2.lua
      - name: Test special workspace routing
        run: |
          lua5.4 - <<'LUA'
          package.path = "dot_config/hypr/?.lua;" .. package.path

          local routes = {}
          local handlers = {}
          local rules = {}
          local existing = {
            { class = "BeeperTexts", title = "Beeper" },
            { class = "org.kde.konsole", title = "shell" },
            { class = "Spotify", title = "Spotify" },
          }

          _G.hl = {
            on = function(event, callback)
              handlers[event] = callback
            end,
            get_windows = function()
              return existing
            end,
            window_rule = function(rule)
              table.insert(rules, rule)
            end,
            dispatch = function(action)
              table.insert(routes, action)
            end,
            dsp = {
              window = {
                move = function(options)
                  return options
                end,
              },
            },
          }

          local routing = require("special_workspaces")

          assert(#rules == 3, "music, Beeper and terminal static rules should be registered")
          assert(rules[1].workspace == "special:music")
          assert(rules[2].workspace == "special:beeper")
          assert(rules[3].workspace == "special:terminal")
          assert(rules[2].match.class:find("BeeperTexts", 1, true))
          assert(rules[3].match.class:find("org.kde.konsole", 1, true))
          assert(rules[3].match.class:find("footclient", 1, true))

          assert(routing.workspace_for_window({ class = "Beeper" }) == "beeper")
          assert(routing.workspace_for_window({ class = "BeeperTexts" }) == "beeper")
          assert(routing.workspace_for_window({ class = "com.beeper.Beeper" }) == "beeper")
          assert(routing.workspace_for_window({ class = "electron", title = "Beeper" }) == "beeper")
          assert(routing.workspace_for_window({ class = "org.kde.konsole" }) == "terminal")
          assert(routing.workspace_for_window({ class = "footclient" }) == "terminal")
          assert(routing.workspace_for_window({ class = "gnome-terminal-server" }) == "terminal")
          assert(routing.workspace_for_window({ class = "URxvt" }) == "terminal")
          assert(routing.workspace_for_window({ class = "Spotify" }) == "music")
          assert(routing.workspace_for_window({ class = "firefox", title = "Beeper - search" }) == nil)

          assert(#routes == 3, "existing windows should be reconciled on load")
          assert(routes[1].workspace == "special:beeper")
          assert(routes[2].workspace == "special:terminal")
          assert(routes[3].workspace == "special:music")
          assert(routes[1].follow == false)
          assert(routes[1].window == existing[1])

          assert(type(handlers["window.open"]) == "function")
          assert(type(handlers["window.class"]) == "function")

          handlers["window.open"]({ class = "xterm", title = "shell" })
          assert(routes[#routes].workspace == "special:terminal")

          handlers["window.class"]({ class = "com.beeper.Beeper", title = "Beeper" })
          assert(routes[#routes].workspace == "special:beeper")

          print("Hyprland special workspace routing tests passed")
          LUA
