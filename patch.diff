--- a/dot_config/hypr/special_workspaces.lua
+++ b/dot_config/hypr/special_workspaces.lua
@@ -14,30 +14,36 @@
   { name = "kjules", key = "SUPER + J" },
 }

-local terminalClasses = {
-  ["alacritty"] = true,
-  ["foot"] = true,
-  ["footclient"] = true,
-  ["kitty"] = true,
-  ["com.mitchellh.ghostty"] = true,
-  ["ghostty"] = true,
-  ["org.wezfurlong.wezterm"] = true,
-  ["wezterm"] = true,
-  ["terminator"] = true,
-  ["konsole"] = true,
-  ["org.kde.konsole"] = true,
-  ["xfce4-terminal"] = true,
-  ["gnome-terminal"] = true,
-  ["gnome-terminal-server"] = true,
-  ["org.gnome.terminal"] = true,
-  ["org.gnome.console"] = true,
-  ["mate-terminal"] = true,
-  ["lxterminal"] = true,
-  ["urxvt"] = true,
-  ["rxvt-unicode"] = true,
-  ["rxvt"] = true,
-  ["xterm"] = true,
+local terminalPriority = {
+  "alacritty",
+  "foot",
+  "footclient",
+  "kitty",
+  "com.mitchellh.ghostty",
+  "ghostty",
+  "org.wezfurlong.wezterm",
+  "wezterm",
+  "terminator",
+  "konsole",
+  "org.kde.konsole",
+  "qterminal",
+  "xfce4-terminal",
+  "gnome-terminal",
+  "gnome-terminal-server",
+  "org.gnome.terminal",
+  "org.gnome.console",
+  "mate-terminal",
+  "lxterminal",
+  "urxvt",
+  "rxvt-unicode",
+  "rxvt",
+  "xterm",
 }
+
+local terminalClasses = {}
+for i, class in ipairs(terminalPriority) do
+  terminalClasses[class] = i
+end

 local classRoutes = {
   music = {
--- a/dot_config/hypr/hyprland.lua.tmpl
+++ b/dot_config/hypr/hyprland.lua.tmpl
@@ -43,21 +43,8 @@
 -- Terminal candidates in preference order. qterminal is resolved from PATH first;
 -- remaining candidates are paths discovered by chezmoi.
 local candidateTerminals = {
-    { name = "qterminal", path = "qterminal" },
-{{- if ne (index . "xtermLocation" | default "") "" }}
-    { name = "xterm", path = [=[{{.xtermLocation}}]=] },
-{{- end }}
-{{- if ne (index . "rxvtUnicodeLocation" | default "") "" }}
-    { name = "rxvt-unicode", path = [=[{{.rxvtUnicodeLocation}}]=] },
-{{- end }}
-{{- if ne (index . "urxvtLocation" | default "") "" }}
-    { name = "urxvt", path = [=[{{.urxvtLocation}}]=] },
-{{- end }}
-{{- if ne (index . "konsoleLocation" | default "") "" }}
-    { name = "konsole", path = [=[{{.konsoleLocation}}]=] },
-{{- end }}
-{{- if ne (index . "ghosttyLocation" | default "") "" }}
-    { name = "ghostty", path = [=[{{.ghosttyLocation}}]=] },
+{{- if ne (index . "alacrittyLocation" | default "") "" }}
+    { name = "alacritty", path = [=[{{.alacrittyLocation}}]=] },
 {{- end }}
 {{- if ne (index . "footLocation" | default "") "" }}
     { name = "foot", path = [=[{{.footLocation}}]=] },
@@ -65,8 +52,8 @@
 {{- if ne (index . "kittyLocation" | default "") "" }}
     { name = "kitty", path = [=[{{.kittyLocation}}]=] },
 {{- end }}
-{{- if ne (index . "alacrittyLocation" | default "") "" }}
-    { name = "alacritty", path = [=[{{.alacrittyLocation}}]=] },
+{{- if ne (index . "ghosttyLocation" | default "") "" }}
+    { name = "ghostty", path = [=[{{.ghosttyLocation}}]=] },
 {{- end }}
 {{- if ne (index . "weztermLocation" | default "") "" }}
     { name = "wezterm", path = [=[{{.weztermLocation}}]=] },
@@ -74,6 +61,10 @@
 {{- if ne (index . "terminatorLocation" | default "") "" }}
     { name = "terminator", path = [=[{{.terminatorLocation}}]=] },
 {{- end }}
+{{- if ne (index . "konsoleLocation" | default "") "" }}
+    { name = "konsole", path = [=[{{.konsoleLocation}}]=] },
+{{- end }}
+    { name = "qterminal", path = "qterminal" },
 {{- if ne (index . "xfce4TerminalLocation" | default "") "" }}
     { name = "xfce4-terminal", path = [=[{{.xfce4TerminalLocation}}]=] },
 {{- end }}
@@ -86,25 +77,34 @@
 {{- if ne (index . "lxterminalLocation" | default "") "" }}
     { name = "lxterminal", path = [=[{{.lxterminalLocation}}]=] },
 {{- end }}
+{{- if ne (index . "urxvtLocation" | default "") "" }}
+    { name = "urxvt", path = [=[{{.urxvtLocation}}]=] },
+{{- end }}
+{{- if ne (index . "rxvtUnicodeLocation" | default "") "" }}
+    { name = "rxvt-unicode", path = [=[{{.rxvtUnicodeLocation}}]=] },
+{{- end }}
+{{- if ne (index . "xtermLocation" | default "") "" }}
+    { name = "xterm", path = [=[{{.xtermLocation}}]=] },
+{{- end }}
 }

 -- Fallback terminal names for runtime PATH search in Lua:
 local runtimeFallbackTerminals = {
-    "qterminal",
-    "xterm",
-    "rxvt-unicode",
-    "urxvt",
-    "konsole",
-    "ghostty",
+    "alacritty",
     "foot",
     "kitty",
-    "alacritty",
+    "ghostty",
     "wezterm",
     "terminator",
+    "konsole",
+    "qterminal",
     "xfce4-terminal",
     "gnome-terminal",
     "mate-terminal",
     "lxterminal",
+    "urxvt",
+    "rxvt-unicode",
+    "xterm",
 }

 local function isExecutable(path)
