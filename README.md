Wait, GitHub's package registry uses `org.opencontainers.image.source` to map a package to a repository, but by default, it pulls the `README.md` at the root of the repository.
Is there a way to link to a subdirectory README?
