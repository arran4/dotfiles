#!/bin/bash
# Seed shell histories with common initialization commands

# gh auth login
echo "gh auth login -h github.com -w -p https" >> ~/.bash_history
echo "gh auth login -h github.com -w -p https" >> ~/.zsh_history
